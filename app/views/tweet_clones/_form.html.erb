<%= form_with(model: tweet_clone) do |form| %>
  <% if tweet_clone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tweet_clone.errors.count, "error") %> prohibited this tweet_clone from being saved:</h2>

      <ul>
        <% tweet_clone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tweet_clone, style: "display: block" %>
    <%= form.text_area :tweet_clone %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
